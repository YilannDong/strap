{
  "_note": "Figma node <-> code component map. Populated by `straps sync` via the Figma MCP (get_code_connect_map). Empty until a Figma file with Code Connect is linked — the Starter-plan MCP quota / desktop-selection blocked this, so the component registry is currently sourced from .straps/source/components.*.json instead.",
  "map": {}
}
